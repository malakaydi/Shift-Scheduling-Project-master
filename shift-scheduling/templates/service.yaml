apiVersion: v1
kind: Service
metadata:
  name: {{ include "shift-scheduling.fullname" . }}
  labels:
    {{- include "shift-scheduling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shift-scheduling.selectorLabels" . | nindent 4 }}
